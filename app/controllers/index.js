const managerCont = require('./manager.controller');
const staffCont  = require('./staff.controller');
const dashCont = require('./dashboard.controller')

module.exports = {
    managerCont,
    staffCont,
    dashCont
    
};